# Skills

Put local Codex skills in this directory. Each skill should live in its own
subdirectory with a `SKILL.md` file.

Example:

```text
skills/.codex/skills/my-skill/SKILL.md
```

After running `./ubuntu.sh`, this package is stowed into:

```text
~/.codex/skills
```
