# Additional Resources

For a nightly's built-in surface, inspect the compiler source's `src/build/roc/Builtin.roc`. For syntax examples, consult `test/echo/all_syntax_test.roc` in the Roc repository. The community `roc-init` getting-started guide may be useful, but confirm all syntax and commands against the installed nightly and project sources.
